import { ILoadResult } from '../command';
import Config from '../config';
import { Plugin } from './plugin';
export declare class MainPlugin extends Plugin {
    protected skipCache: boolean;
    private tsconfig?;
    private _commandsDir;
    constructor(config: Config);
    load(): Promise<ILoadResult>;
    protected readonly commandsDir: string;
    private fetchTSConfig();
}
