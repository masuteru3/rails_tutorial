import { Config, ConfigOptions } from './config';
export default class CLI {
    private config;
    constructor(config: Config);
    run(argv: string[]): Promise<any>;
    private readonly debug;
    private readonly global;
    private setupHandlers();
    private exitAfterStdoutFlush();
    private flush();
}
export declare function run(argv?: string[], opts?: ConfigOptions): Promise<any>;
export declare function run(opts: ConfigOptions): Promise<any>;
export { ICommandInfo } from './command';
export { Hook } from './hooks';
