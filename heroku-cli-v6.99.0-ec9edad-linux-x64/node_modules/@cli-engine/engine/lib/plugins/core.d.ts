import Config from '../config';
import { IPluginPJSON, Plugin, PluginType } from './plugin';
export declare class CorePlugins {
    private config;
    plugins: CorePlugin[];
    constructor(config: Config);
    submanagers(): Promise<CorePlugin[]>;
    init(): Promise<void>;
    private pjsonPath(name);
    private nextRoot();
}
export declare class CorePlugin extends Plugin {
    type: PluginType;
    pjson: IPluginPJSON;
}
