import Config from '../config';
import { Plugin } from './plugin';
export declare class Builtin extends Plugin {
    constructor(config: Config);
    readonly commandsDir: string;
    protected commandIDsFromDir(): Promise<string[]>;
}
